/**
 * @file Converts the weather information from Celsius to Fahrenheit and vice-versa
 */

const changeUnits = (tempUnits) => {};

/**
 * Given a Fahrenheit temperature, change to Celsius
 * C = (F - 32) / 1.8
 */
const changeCelsius = () => {};

/**
 *
 */
const changeFahrenheit = () => {};

const updateCurrent = (tempUnit, current) => {};
